import * as types from './../constants/ActionTypes';
var initialState = [];

const category_news = (state = initialState, action) => {
    switch(action.type){
        default: return [...state];
    }
}

export default category_news;
